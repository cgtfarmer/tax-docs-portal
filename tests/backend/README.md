# Playwright

Run all tests:

```sh
npm run test
```

Run a single test:

```sh
npx playwright test -g 'create user'
```

Run formatter:

```sh
npm run format
```

Run linter:

```sh
npm run lint
```
